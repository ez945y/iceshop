# 刨冰人格測驗 🍧

挑配料，測人格 + 戀愛契合度的單頁互動測驗。輕鬆、療癒、可分享。

- **純前端、零依賴、零建置**：整個測驗就是一支 `index.html`（inline CSS/JS/SVG），離線也能開。
- **無後端**：雙人契合度靠「冰友代碼」——把選擇編碼成一串字，對方在自己的瀏覽器輸入後本地解碼比對，沒有任何伺服器留存資料。
- **精神**：不合就是「分開吃，各自美麗」，所有契合度文案都給正面出口。

## 開發

直接用瀏覽器打開 `index.html` 即可，沒有建置步驟。

## 部署

以 GitHub Pages 托管（靜態站）。
